name: Ping Render Server

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: |
          curl -s https://jade-ruan.onrender.com > /dev/null
          echo "Ping enviado!"
